{
  "name": "dots-and-boxes",
  "private": true,
  "packageManager": "pnpm@9.2.0",
  "scripts": {
    "build:wasm": "wasm-pack build wasm --target web --out-dir pkg --scope dab && rm -f wasm/pkg/.gitignore",
    "dev": "pnpm --filter @dab/web dev",
    "build": "pnpm build:wasm && pnpm --filter @dab/web build",
    "lint": "pnpm --filter @dab/web lint",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3.6.2"
  }
}
